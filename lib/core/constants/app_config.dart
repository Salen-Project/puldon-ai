/// Application Configuration
class AppConfig {
  // API Configuration
  static const String apiBaseUrl = 'https://151.245.140.91';

  // Mode: Set to false to use real API, true for mock data
  static const bool useMockData = false;  // ✅ API mode enabled - pulling real data

  // Debug mode
  static const bool enableApiMonitor = true;
  static const bool enableApiLogging = true;
}

